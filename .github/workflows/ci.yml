name: CI

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

jobs:
  check:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Type-check frontend
        run: pnpm exec tsc --noEmit

      - name: Check Rust formatting
        working-directory: src-tauri
        run: cargo fmt --check

      - name: Check Rust code
        working-directory: src-tauri
        run: cargo check

      - name: Run frontend tests
        run: pnpm test

      - name: Run Rust tests
        working-directory: src-tauri
        run: cargo test

  macos-e2e:
    needs: check
    runs-on: macos-latest
    # This suite drives the real compiled .app through System Events —
    # unlike the (fast, deterministic) checks above, it's brand new and has
    # never run outside this workflow. Non-blocking until a green run
    # confirms the AppleScript selectors hold up on an actual macOS runner;
    # flip this to blocking once that's confirmed. See e2e/macos/README.md.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable

      # Belt and suspenders: this job hit "rustup could not choose a
      # version of cargo to run... no default is configured" even though
      # the toolchain step above ran — the `check` job (same action, same
      # runner image) has never hit this. Whatever's inconsistent about it
      # on this job's runner, explicitly setting the default removes the
      # ambiguity rustup was complaining about.
      - name: Ensure a default Rust toolchain is set
        run: rustup default stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Run macOS UI E2E suite
        run: ./e2e/macos/run.sh
